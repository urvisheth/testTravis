language: python
python:
  - "3.5"
script:
  - python hello.py
deploy:
  provider: s3
  access_key_id: 
    secure :${ACCESS_API_KEY}
  secret_access_key:
    secure :${ACCESS_SECERT_KEY}
  bucket: urvishethtest11

